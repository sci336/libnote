// Lexical is the default desktop-first editor path. Keep the old PageEditor
// fallback intact; set this to false if Lexical needs to be rolled back.
// Mobile, narrow viewport, and touch QA still need a future follow-up pass.
export const USE_LEXICAL_EDITOR = true;
